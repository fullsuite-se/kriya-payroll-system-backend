datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

/**
 * We removed the user table,
 * since the hris backend will be the authentication provider
 * for all the apps in the kriya ecosystem.
 */

model Management {
  management_id String   @id @default(uuid()) @db.Char(36)
  user_id       String   @db.Char(36)
  company_id    String   @db.Char(36)
  created_at    DateTime @default(now()) @db.Timestamp(0)
  updated_at    DateTime @default(now()) @db.Timestamp(0)

  company Company @relation(fields: [company_id], references: [company_id])

  @@map("managements")
}

model EmployeeDesignation {
  employee_designation_id String   @id @default(uuid()) @db.Char(36)
  employee_id             String   @unique @db.Char(36)
  company_id              String   @db.Char(36)
  created_at              DateTime @default(now()) @db.Timestamp(0)
  updated_at              DateTime @default(now()) @db.Timestamp(0)

  employee Employee @relation(fields: [employee_id], references: [employee_id])
  company  Company  @relation(fields: [company_id], references: [company_id])

  @@map("employee_designations")
}
